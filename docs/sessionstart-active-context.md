# SessionStart active context

New coding sessions should not start cold.

At session start, generate a compact `active-context.md` with:

- recent relevant sessions;
- current repo status if available;
- open handoffs;
- recent lessons;
- relevant skills/checklists.

If the central memory host is unavailable, fail quietly and continue. Do not block coding startup.
